<h1></h1>
<div class="head-show">
    <%= render @head, :part => @head %>
</div>
<div class="torso-show">
    <%= render @torso, :part => @torso %>
</div>
<div class="legs-show">
    <%= render @legs, :part => @legs %>
</div>
<div class="feet-show">
    <%= render @feet, :part => @feet %>
</div>
<br>

<%= link_to 'New Part', new_part_path %>
